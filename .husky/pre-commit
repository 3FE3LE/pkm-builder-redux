#!/usr/bin/env sh

files="$(git diff --cached --name-only --diff-filter=ACMR | grep -E '^(app|components|hooks|lib|tests)/.*\.(ts|tsx)$' || true)"

if [ -z "$files" ]; then
  exit 0
fi

pnpm exec vitest related --run $files
